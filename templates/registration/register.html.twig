{% extends 'base.html.twig' %}

{% block titre %}
    <div class="jumbotron" style="margin: 110px; background: none; text-align: center; margin-right:59% ">
        <h1 class="display-4 pb-5">S'enregistrer</h1>


    </div>
{% endblock %}

{% block body %}


    {{ form_start(registrationForm) }}
        {{ form_row(registrationForm.nom) }}
        {{ form_row(registrationForm.prenom) }}

        {{ form_row(registrationForm.mail) }}
        {{ form_row(registrationForm.Password) }}
        {{ form_row(registrationForm.idClasse) }}
        {{ form_row(registrationForm.agreeTerms) }}

        <button class="button">Register</button>


    {{ form_end(registrationForm) }}
    <a href="{{ path("rgpd") }}" class="button">Le RGPD, qu'est ce que c'est ?</a>
{% endblock %}
